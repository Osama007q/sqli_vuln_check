Few Instructions to reach this GPT Security interface and then be possible to make the analysis: 
1-	Firstly you need to make sure that you have Node.js Installed on your machine and this could be done easily by visiting https://nodejs.org/ and then open the installer to finish the process.
2-	The second step is to install npm(Node Package Manager) and that is done by running the following order in Terminal: npm install .
3-	Then after decompress the file I have attached along with the report, you simply (Run the server) using the Terminal: node app.mjs .
4-	The last step to get reached to that interface is by typing : http://localhost:8081/
And hop you are in the interface of GPT Security!!
